function findJudge(n: number, trust: number[][]): number {

    if (n === 1 && trust.length === 0) return 1;

    let judge = trust[0][1];
    let found = true;
    let i = 0;
    while(!found && i < n) {

    }
};